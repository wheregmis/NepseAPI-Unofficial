[[source]]
name = "NepseApi"
url = ""
verify_ssl = true

[dev-packages]

[packages]
Flask = ">=3.0.3"
pywasm = ">=1.0.8"
httpx = {version = ">=0.27.2", extras = ["http2"]}
tqdm = ">=4.66.5"
fastapi = ">=0.115.5"
uvicorn = ">=0.32.1"
nepse = {git = "https://github.com/basic-bgnr/NepseUnofficialApi.git", ref = "master"}

[requires]
python_version = "3.8"  # Adjust this to match your project's Python version
